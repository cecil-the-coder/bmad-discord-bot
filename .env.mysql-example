# MySQL Deployment - Required Environment Variables Only
# All other configuration values are managed in the database for cloud-native deployments

# Discord Bot Token - Required
# Get this from Discord Developer Portal: https://discord.com/developers/applications
BOT_TOKEN=your_discord_bot_token_here

# Database Configuration - Required for MySQL
DATABASE_TYPE=mysql

# MySQL Configuration - Required
MYSQL_HOST=localhost
MYSQL_PORT=3306
MYSQL_DATABASE=bmad_bot
MYSQL_USERNAME=bmad_user
MYSQL_PASSWORD=your_mysql_password_here
MYSQL_TIMEOUT=30s

# AI Provider Selection - Required
# Options: gemini, ollama
AI_PROVIDER=gemini

# Gemini CLI Path - Required when using Gemini
GEMINI_CLI_PATH=/path/to/gemini-cli

# Optional: Override default message recovery window (default: 5 minutes)
# MESSAGE_RECOVERY_WINDOW_MINUTES=5

# Optional: Override default database path for SQLite fallback
# DATABASE_PATH=./data/bot_state.db

# NOTE: All other configuration values (rate limits, feature flags, AI preferences, 
# status settings, etc.) are stored in the MySQL database and can be managed 
# dynamically without restarting the application.
# 
# To set these values:
# 1. Start the application - it will migrate existing environment variables to database
# 2. Use the ConfigService API or direct database access to modify configuration
# 3. Changes take effect within 1 minute (configurable reload interval)